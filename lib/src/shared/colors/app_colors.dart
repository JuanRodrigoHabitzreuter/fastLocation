// Cores usadas no app.
// Import: flutter/material.dart
import 'package:flutter/material.dart';

/// Cores utilizadas no app
class AppColors {
  static const Color primary = Colors.deepOrange; // laranja padrão
  static const Color secondary = Color(0xFF001F54); // azul marinho
  static const Color background = Colors.deepOrange;
}
